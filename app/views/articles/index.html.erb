<div class="container">
  <%= render 'tabs'%>

  <% @articles.each do |article| %><%# 部分テンプレートには値を渡すことができる %>
    <%= render 'article', article: article %>
  <% end %>
  <%= link_to new_article_path do %>
    <div class="float_btn">
      <i class="fa fa-plus"></i>
    </div>
  <% end %>
</div>
