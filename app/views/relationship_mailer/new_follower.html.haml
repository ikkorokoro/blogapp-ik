%p #{@user.email}さん
%p 以下のユーザーにフォローされました。

= link_to 'プロフィールを確認する', account_url(@follower)
/* user_path => '/users 
  user_url => 'http://localhost:3000/users' 
  メールアドレスにからリンクを開くので全てのurlがないとページが開けない*/
